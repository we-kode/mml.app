/// Repeat modes that are allowed by the player.
enum PlayerRepeatMode {
  /// No repeat, stop at the end.
  none,

  /// Repeat all records, restart at the end with the first record.
  all,

  /// Repeat one single record.
  one,
}
